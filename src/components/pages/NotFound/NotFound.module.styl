.not-found {
    justify-content: space-between;
}

.top {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.image {
    width: 250px;
    height: 354px;
}

.heading {
    margin: 0 0 8px;
    font-weight: bold;
    font-size: 96px;
    line-height: 102px;
}

.description {
    max-width: 70%;
    margin: 0;
    padding: 0 10px;
    font-weight: 500;
    font-size: $fs-s;
    line-height: 18px;
    text-align: center;
}
