// Transitions
$trans-300 = 0.3s;

// Offsets
$offset-x = 4px;
$offset-2x = $offset-x * 2;
$offset-3x = $offset-x * 3;
$offset-4x = $offset-x * 4;
$offset-5x = $offset-x * 5;
$offset-6x = $offset-x * 6;
$offset-8x = $offset-x * 8;
$offset-9x = $offset-x * 9;
$offset-10x = $offset-x * 10;
$offset-11x = $offset-x * 11;
$offset-12x = $offset-x * 12;

// Fonts
$fs-xl = 24px;
$fs-l = 20px;
$fs-m = 18px;
$fs-s = 16px;
$fs-xs = 14px;
$fs-xxs = 12px;

// Sizes
$size-header = 56px;
$size-nav = 54px;
$size-button = 48px;
$size-card = 160px;

// Border radius
$br-card = 4px;

// Colors
$color_main = var(--color_main);
$color_main-008 = var(--color_main-008);
$color_main-020 = var(--color_main-020);
$color_secondary = var(--color_secondary);
$color_background = var(--color_background);
$color_surface = var(--color_surface);
$color_on-surface = var(--color_on-surface);
$color_error = var(--color_error);
$color_white = var(--color_white);
$color_black = var(--color_black);
$color_black-060 = var(--color_black-060);
$color_active = var(--color_active);
$color_primary = var(--color_primary);
$color_light = var(--color_light);
$color_navy-hover = var(--color_navy-hover);
